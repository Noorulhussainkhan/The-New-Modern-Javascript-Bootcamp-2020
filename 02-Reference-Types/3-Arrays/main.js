// A collection of all data inside the brackets is called Array

let hobbies = ['Coding', 'Cricket', 'Watching Movies', 'Singing Songs']
console.log(hobbies);

// You can write anything you want inside Array === Brackets

let primitiveTypes = ['String', true, undefined, null, 2, 2 + 2]
console.log(primitiveTypes)

function greet() {
    console.log('Hello Every One')
}

let person = {
    name: 'Noor ul hussain',
    age: 17
}

let fruites = ['Orange', 'Mango', 'Apple', 'Banana']

let referenceTypes = [greet, person, fruites]