// Undefined is given by browser  -- E.G === When we write a variable but diden't assign a value to it so the JS-engine will show us (undefined)
// &
// Null set by programmer -- E.G == When we write variable but later one we want to change the value of var soo we set the value to (Null)


const name; // undefined
console.log(name);

const anotherName = null // set by programmer
console.log(anotherName)